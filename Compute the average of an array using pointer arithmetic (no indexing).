#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int arr[n];
    int *ptr=arr;
    int s=0;
    for(int i=0;i<n;i++)
    {
        scanf("%d",ptr+i);
    }
    for(int i=0;i<n;i++)
    {
       s=s+*(ptr+i);
    }
    float avg=(float)s/n;
    printf("%f",avg);
}
