#include<stdio.h>
int main()
{
    char str[1000];
    int len=0;
    //scanf("%[^\n]",str);
    fgets(str,sizeof(str),stdin);
    while(str[len]!='\0')
    {
        if(str[len]=='\n')
        break;
        
        len++;
    }
    printf("%d",len);
    return 0;
}
