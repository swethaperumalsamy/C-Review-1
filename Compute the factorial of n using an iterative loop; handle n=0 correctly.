/*#include<stdio.h>
int main()
{
    long long fact=1;
    int n;
    scanf("%d",&n);
    for(int i=1;i<=n;i++)
    {
        fact=fact*i;
    }
    printf("%lld",fact);
    return 0;
}*/


#include<stdio.h>
long long factorial(int n)
{
    if (n==0)
    {
        return 1;
    }
    return n*factorial(n-1);
}
int main()
{
    int n;
    scanf("%d",&n);
    printf("%lld",factorial(n));
}
